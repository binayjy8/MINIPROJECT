<% layout ("/layouts/boilerplate") %>
<body>
    <h3>All Listings</h3>
    <% for(let listing of allListings) { %>
    <div class="card" style="width: 18rem;">
        <img src="<%= listing.image %>" class="card-img-top" alt="listing_image">
        <div class="card-body">
          <p class="card-text">
            <%= listing.title %> <br>
            <%= listing.price %>
          </p>
        </div>
      </div>
    <% } %>
    <ul>
        <% for(let listing of allListings) { %>
            <li><a href="/listings/<%= listing._id %>"></a></li>
        <% } %>
    </ul>
</body>